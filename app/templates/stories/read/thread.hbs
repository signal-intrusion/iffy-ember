{{#with model as |thread|}}

  {{#with thread.story.firstScene as |firstScene|}}
    {{scene-display scene=firstScene showChoices=hasThreadScenes onSelectChoice=(action 'addSceneToThread')}}
  {{/with}}

  {{#each thread.threadScenes as |threadScene|}}
    {{threadScene.scene.text}}
    <br />
  {{/each}}

{{/with}}
